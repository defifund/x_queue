class CreateXThreadTweets < ActiveRecord::Migration[<%= ActiveRecord::Migration.current_version %>]
  def change
    create_table :x_thread_tweets do |t|
      t.text     :content,              null: false
      t.string   :access_token,         null: false
      t.string   :access_token_secret,  null: false
      t.integer  :status,               null: false, default: 0
      t.datetime :scheduled_at
      t.datetime :posted_at
      t.string   :x_tweet_id
      t.string   :thread_id
      t.integer  :thread_position
      t.references :source,             polymorphic: true, index: true
      t.timestamps
    end

    add_index :x_thread_tweets, :status
    add_index :x_thread_tweets, :thread_id
    add_index :x_thread_tweets, :scheduled_at
    add_index :x_thread_tweets, :x_tweet_id, unique: true, where: "x_tweet_id IS NOT NULL"
  end
end
